# Identity for GitHub remotes only. Loaded by the includeIf blocks in
# git/.gitconfig, so a fresh clone needs no per-repo `git config --local`.
#
# Target path: ~/.config/git/github.inc
#
# The 64059464+ prefix is required: this GitHub account was created after
# 2017-07-18, so the bare noreply form is not attributed to it.
[user]
	name = Zhang Houbin
	email = 64059464+zhanghb18@users.noreply.github.com
